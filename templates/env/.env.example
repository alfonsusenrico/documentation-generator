# App
APP_ENV=production
PORT={{APP_PORT}}
LOG_LEVEL=info

# Deploy image (used by compose)
IMAGE_NAME={{IMAGE_NAME}}
IMAGE_TAG=sha-<commit>

# Optional (add project-specific vars below)
DATABASE_URL=
REDIS_URL=
SENTRY_DSN=
